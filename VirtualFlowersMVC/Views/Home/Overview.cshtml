@model List<VirtualFlowersMVC.Models.OverViewViewModel>
@{
    ViewBag.Title = "Index";


}

<h2>Index</h2>

@using (Html.BeginForm())
{
    @*foreach (var item in Model)
    {
        <table>
            <tr>
                <td>
                    @Html.DisplayFor(var => item.Id)
                </td>
                <td>
                    <a href="@item.Url">@item.Url</a>
                </td>
                @*<td>
                        @Html.DisplayFor(modelItem => item.BestOf3)
                    </td>*@

                @*<td>
                    @Html.CheckBoxFor(modelItem => item.Checked)
                </td>
            </tr>
        </table>
    }*@


    for (var i = 0; i < Model.Count(); i++)
    {


        <table>
            <tr>
                <td>
                    @Html.HiddenFor(it => it[i].Id)
                    @Html.HiddenFor(it => it[i].Name)
                  @*  @Html.DisplayFor(it => it[i].Url)*@

                </td>

                <td>
                    <a href="@Model[i].Url">@Model[i].Url</a>
                   @* <a href="@item.Url">@item.Url</a>*@
                </td>
                <td>
                    @Html.CheckBoxFor(it => it[i].Checked, new { Style = "vertical-            align:3px}" })
                </td>
            </tr>
        </table>

    }
    <input id="Submit1" type="submit" value="submit" />

} 